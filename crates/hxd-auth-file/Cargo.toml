[package]
name = "hxd-auth-file"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "hxd-ng flat-file account backend (one TOML file per account)"
publish = false

[dependencies]
hxd-core = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
# Writing identity links back into hand-maintained files without losing
# their comments.
toml_edit = "0.22"
hl-identity = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
